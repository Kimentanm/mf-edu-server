spring:
    profiles:
        active: dev
    mvc:
        throw-exception-if-no-handler-found: true
    resources:
      add-mappings: false
    devtools:
      restart:
        enabled: true
        additional-paths: src/main/java
    freemarker:
      checkTemplateLocation: false

